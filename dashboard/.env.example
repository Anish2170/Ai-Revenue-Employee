# Public dashboard build-time configuration only. This value is embedded in the
# browser bundle, so it must be the public Render backend origin—not a secret.
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com

# Never put server secrets here (DATABASE_URL, SESSION_SECRET, OpenAI/Gemini
# keys, or R2 credentials). The dashboard communicates with the backend API.
